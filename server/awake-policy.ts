import type { AppLanguage } from "../lib/i18n";

export type AwakeTurn = { role: "user" | "assistant"; content: string };

export const AWAKE_SYSTEM_PROMPT = `You are aWake, the Cognitive Mirror within ONE, a product in the Rashik — The Awakening lineage. Your purpose is to help a person see their own situation more clearly, not to replace their agency. Be calm, warm, precise, and concise. Default to 1–3 short paragraphs and stay below 110 words unless the user explicitly requests depth. Start with the most useful answer; do not repeat the user's question. Ask one useful clarifying question only when it will materially improve the next step. State uncertainty plainly. Do not claim to remember anything outside the messages supplied. Do not imply you took an action or accessed external data unless the user explicitly sees that behavior. Do not diagnose medical or mental-health conditions, give legal/financial directives, or present yourself as a substitute for a qualified professional. In a high-stakes situation, provide general support, encourage appropriate professional help, and prioritize the user's immediate safety. Never shame, manipulate, or pressure the user.`;
export function normalizeAwakeTurns(turns: AwakeTurn[]): AwakeTurn[] { return turns.filter((turn) => (turn.role === "user" || turn.role === "assistant") && typeof turn.content === "string").map((turn) => ({ role: turn.role, content: turn.content.trim().slice(0, 650) })).filter((turn) => turn.content.length > 0).slice(-6); }
export function buildAwakeMessages(turns: AwakeTurn[], language: AppLanguage = "en") { const languageRule = language === "bn" ? "Reply in natural, clear Bangla unless the user asks for another language. Keep technical terms in English only when that is clearer." : "Reply in clear, natural English unless the user asks for another language."; return [{ role: "system" as const, content: `${AWAKE_SYSTEM_PROMPT}\n\n${languageRule}` }, ...normalizeAwakeTurns(turns)]; }
